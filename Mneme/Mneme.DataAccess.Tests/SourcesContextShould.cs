using Xunit;

namespace Mneme.DataAccess.Tests
{
	public class SourcesContextShould
	{
		[Fact]
		public void CreateDatabaseWhenInitialized()
		{
			/*			using var db = new Context();
						var fixture = new Fixture();
						db.Database.Migrate();
						_ = db.MnemeSources.Add(new Model.Sources.MnemeSource() { Title = "a", Id = fixture.Create<string>() });
						_ = db.SaveChanges();*/
		}
	}
}
